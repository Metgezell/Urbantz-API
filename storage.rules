rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow uploads to documents folder
    match /documents/{allPaths=**} {
      allow read, write: if true; // TODO: Add proper authentication
    }
    
    // Allow uploads to training folder
    match /training/{allPaths=**} {
      allow read, write: if true; // TODO: Add proper authentication
    }
  }
}

